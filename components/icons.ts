import { Eye, EyeOff, Loader2, Search, SquarePen, type LucideIcon } from 'lucide-react-native'

export const Icons: Record<string, LucideIcon> = {
  eye: Eye,
  eyeOff: EyeOff,
  search: Search,
  spinner: Loader2,
  write: SquarePen,
}